# WeatherAppBackend
This is the backend server of the weather alerter.


1. Clone the repo using ```git clone https://github.com/YuvrajMann/WeatherAppBackend.git```
2. Move to the project root directory and type ```yarn install``` to install all dependicies
3. After that run the command ```yarn start``` to run the sever (Server will run at port 3000) 
